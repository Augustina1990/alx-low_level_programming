#include "main.h"
#include <string.h>
#include <stdio.h>
 /**
  *strlen - checks the length of a string
  *@s: input
  *Return: Always 0 (success)
  */
int _strlen(char *s)
{
	int lenght;

	lenght = strlen(s);
	return (lenght);
}
